# Copy this file to `.npmrc` and set GITHUB_TOKEN in your shell env.
# The PAT needs `read:packages` (for installing) or `write:packages` (for publishing locally).
# Generate one at: https://github.com/settings/tokens

@dump-hr:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
